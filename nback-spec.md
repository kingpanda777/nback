# Nバック課題アプリ 仕様書

自分専用。市販アプリでは難易度の上がり方と記録の取り方が思いどおりにならないため、
全部自分で調整できるものを作る。

---

## 全体方針

- **刺激列の生成と、その提示方法を分離する。** モダリティは設定値であって分岐ではない。
  ここを最初から分けておけば、音を足すのは提示側に処理を1つ増やすだけで済む。
  あとからデュアル対応をねじ込むと判定ロジックまで書き直しになる。
- 第1段階は視覚のみ。音・デュアルは第2段階。
- 成績は履歴として残す。

---

## 用語

| 語 | 意味 |
|---|---|
| N | 何個前の刺激と比較するか |
| 試行 (trial) | 刺激1個の提示 |
| ブロック | 1回分のセッション（試行の連続） |
| ターゲット | Nバック前と一致する刺激 |
| ヒット | ターゲットに正しく反応した |
| ミス | ターゲットに反応しなかった |
| 誤警報 | 非ターゲットに反応した |
| 正棄却 | 非ターゲットに正しく反応しなかった |

---

## 刺激列の生成

- `generate_sequence(n, trials, target_rate, alphabet) -> list`
- ターゲット率は 25〜30% が標準。**設定可能にする。**
- 乱数シードを記録し、同じ列で再挑戦できるようにする。自己ベストの検証に効く。
- **ひっかけ (lure)** ＝ N-1 や N+1 の位置で一致する刺激。難易度が大きく上がるため、
  第1段階では混ぜない。設定項目としてだけ用意し、既定は OFF。

---

## モダリティ

1. **visual-position** — 3×3グリッドに四角が光る。中央を除いた8マスを使うのが標準
2. **visual-number** — 1〜9の数字を中央に表示（これまで慣れている形式）
3. **audio-letter** — 読み上げられる文字。子音のみ（C, H, K, L, Q, R, S, T）が定番
4. **dual** — position + audio を同時提示。判定は独立した2キーで別々に行う

---

## タイミング

- 刺激提示 500ms、間隔 2500ms（1試行あたり計 3000ms）が標準。**設定可能にする。**
- 反応の受付は次の刺激が出るまで。
- 1ブロック = N + 20 試行 程度。

---

## 入力

- 単一モダリティ: スペースキー
- デュアル: 位置 = A キー、音 = L キー
- 一致しないときは押さない（no-go 方式）
- 同一試行内の二重押しは無視

---

## 判定と指標

**正答率だけでは評価しない。** 「一致と思ったら全部押す」戦略で数字が良く見えてしまい、
あとから自分の伸びを見るときに使えなくなる。

- ヒット率 = ヒット ÷ ターゲット総数
- 誤警報率 = 誤警報 ÷ 非ターゲット総数
- この2つを必ず別々に記録する
- 余力があれば d'（弁別力）も算出

---

## 記録

保存項目:

- 日時
- N
- モダリティ
- 試行数 / ターゲット数
- ヒット / ミス / 誤警報 / 正棄却
- 平均反応時間
- 乱数シード
- 設定値（提示時間・間隔・ターゲット率・lure の有無）

形式は 1行1ブロックの JSON Lines か SQLite。
履歴画面では、日付ごとの最高N と、モダリティ別の推移が見られること。

---

## 適応的難易度（第2段階以降）

Jaeggi らの方式: ブロック終了時に「ミス＋誤警報」が 3件以下なら N+1、5件以上なら N-1。

第1段階では N を手動選択とし、慣れてから自動化する。

---

## 着手前に決めること

1. **動作環境** — ブラウザ単体（HTML/JS）か、ニュースアプリと同様に Python + 静的サイトか
2. **音声の出し方** — Web Speech API はブラウザ差が大きい。
   事前録音した音声ファイルを持つほうが安定する（第2段階の話だが、
   第1段階の構成を決める時点で方針だけ決めておきたい）
3. **保存先** — ブラウザ内に持つか、ローカルのファイルに書くか

---

## 第1段階のスコープ

- 視覚（数字・位置）
- N は手動選択
- 1ブロック実行 → 結果表示 → 履歴保存

音とデュアルは含めない。ただし上記「全体方針」のとおり、
あとから足せる構造にしておくこと。

## やらないこと

ランキング、SNS共有、課金、他人との比較。
