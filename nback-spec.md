# Nバック課題アプリ 仕様書

自分専用。市販アプリでは難易度の上がり方と記録の取り方が思いどおりにならないため、
全部自分で調整できるものを作る。

第1段階は実装済み。この仕様書は実装に合わせて更新してある。
第2段階（音・デュアル）はまだ。実装側の話は [README.md](README.md)。

---

## 全体方針

- **刺激列の生成と、その提示方法を分離する。** モダリティは設定値であって分岐ではない。
  ここを最初から分けておけば、音を足すのは提示側に処理を1つ増やすだけで済む。
  あとからデュアル対応をねじ込むと判定ロジックまで書き直しになる。
- **応答方式も同じく設定値にする。** 提示中に押す方式と、自分のペースで進む方式を、
  同じ刺激列生成の上に載せる。
- **自分のペースの課題は1つの実行部で回す。** 出すものと答えさせ方だけを差し替える。
  課題を増やすときに進行・採点・記録を書き直さない。
- **チャンネル（提示系統）は最初から配列で持つ。** 単一モダリティは1本、デュアルは2本。
  実行部はどちらでも同じコードで回る。
- 第1段階は視覚のみ。音・デュアルは第2段階。
- 成績は履歴として残す。

---

## 用語

| 語 | 意味 |
|---|---|
| N | 何個前の刺激と比較するか |
| 試行 (trial) | 刺激1個の提示 |
| ブロック | 1回分のセッション（試行の連続） |
| チャンネル | 提示系統1本。単一は1本、デュアルは2本 |
| 応答方式 | リアルタイム判定 か 自分のペース のどちら |
| 課題 | 自分のペース方式で何をやるか（計算 / 数字 / 位置） |
| ターゲット | Nバック前と一致する刺激 |
| ヒット | ターゲットに正しく反応した |
| ミス | ターゲットに反応しなかった |
| 誤警報 | 非ターゲットに反応した |
| 正棄却 | 非ターゲットに正しく反応しなかった |
| 回答数 | 自分のペース方式で採点する問題数。出題は N + この数 |
| 連続正答 | 頭から何問続けて正解したか |

---

## 応答方式

2つある。設定画面で切り替える。

### リアルタイム判定（no-go）

提示中に、N個前と一致したらボタンかスペースキーを押す。一致しないときは押さない。
Nバック課題そのもの。難易度は N。

### 自分のペース

刺激が1つ出る → 覚える → **N個前に出たものを入力する** → 入力した瞬間に次が出る。
タイマーは使わない。最初の N 問は答える相手がいないので「覚えるだけ」。
出題は N + 回答数。

課題が3つある。流れは同じで、違うのは「何を出すか」と「どう答えさせるか」だけ。

| 課題 | 出るもの | 答え方 |
|---|---|---|
| 計算 | 四則演算の式 | 答えを 0〜9 のボタン |
| 数字 | 0〜9 の数字1つ | その数字を 0〜9 のボタン |
| 位置 | 3×3グリッドのマスが1つ光る | **3×3グリッドの該当マスをタップ**（数字ボタンには置き換えない） |
| 混合 | 試行ごとに数字か位置かが切り替わる | 位置グリッドと数字パッドを左右に並べる。どちらを押したかで種類も決まる |

混合課題は数字が 0〜9、位置は中央を除く8マスの計18種。
数字の範囲は「数字」課題と揃えてある（リアルタイム判定の混合は 1〜9 で別物）。
中央マスは数字の表示に使うので位置には回さない。
種類と値の両方を覚える必要があるので、同じ N でも単独より重い。

計算課題の出題:

- 足し算・引き算・掛け算・割り算の4種類
- **割り算は割り切れるものだけ**（答えから逆算して作るので構造上そうなる）
- **答えはすべて一桁 (0〜9)**。項のほうは桁が増えてよい（`56 ÷ 8`、`12 - 5` など）
- 答えが小さいと式が単純になる（`0 + 0`、素数のときの `1 × 7`）。
  答えを一桁に収める以上は避けられないので、そのままにしてある
- **将来 答えを2桁に広げられるようにしてある。** `answerMax` を 99、`answerDigits` を 2 に。今は一桁に固定

共通:

- 難易度は N。毎試行で新しい刺激を処理しながら N 個ぶん保持し続ける
- 入力はボタンのみ。**キーボードは使わない**（中止の Esc だけ受ける）
- 「1つ戻る」は付けない。前の刺激を出し直すことになり、
  見直しの時間を与えて課題の性質が変わるため。押し間違いは誤答として数える
- 課題のIDはリアルタイム判定のモダリティとは別にしてある
  （同じ「数字」でもリアルタイムは1〜9、こちらは0〜9で記号集合が違う。
  同じIDにすると記録を後から読むときに取り違える）

### 廃止：提示後に入力（すべて覚えて最後に全部答える）

**問題数を決める → その数だけ順に提示 → 全部出たあと、出てきた順に思い出して入力する**
という方式を一時期入れていたが、削除した。

> **Nバックではなく記憶スパン課題だったため。**
> 全部提示してから順に答えるだけなので、N の出番がない。
> N を「末尾の何個を答えなくてよいか」に使うことも試したが、
> それだと N を上げるほど回答数が減るだけで、難易度のつまみとして機能しなかった。
> 結局これは digit span / Corsi block に相当する別の課題で、
> Nバックの訓練という当初の目的から外れていた。
> 「自分のペース」方式なら N が本当に効く（毎試行で新しい刺激を処理しながら保持する）ので、
> そちらに置き換えた。

**過去の記録も残さないことにした。** 短い期間しか使っておらず、
取っておいても比較対象にならないため。
`storage.js` の読み込みの入口で `responseMode: "recall"` の記録を落とし、その場で保存し直す。
`.jsonl` の読み込みからも除くので、古い書き出しを取り込んでも戻らない。
落とすのを入口1か所に寄せてあるので、履歴側は廃止した方式を一切知らなくてよい。

---

## 刺激列の生成

```
generateSequence({ n, trials, targetRate, alphabet, seed, lure })
  -> { symbols, isTarget, isLure, seed, targets }
```

当初は記号の配列だけを返す想定だったが、判定と記録に `isTarget` が要るので一緒に返す。
`isLure` は分析用で、判定には使わない。

- ターゲット率は 25〜30% が標準。**設定可能**（既定 28%）。
  試行数から丸めた**正確な個数**を毎回置く（確率ではなく個数で決める）
- 乱数シードを記録し、同じ列で再挑戦できるようにする。自己ベストの検証に効く
- **ひっかけ (lure)** ＝ N-1 や N+1 の位置に同じ刺激を置く罠。難易度が大きく上がる。
  **全方式で設定でき、既定は OFF。**（詳しくは下の「ひっかけ」の節）
- **`targetRate = 0`** は「ターゲットを置かない」指示。自分のペース方式で使う
  （毎試行が出題なのでターゲットの概念がない）。N は渡す。ひっかけを N±1 に置くのに要るため
- 自分のペース方式では **N個前との一致を禁じない**。禁じると
  「いま出ているものは答えではない」と分かってしまい、選択肢が狭まる
- 直前と同じ記号は避ける（2回出たのか1回だったのか分からなくなるため）。
  ただし N=1 のときの1個前は「答えそのもの」なので縛らない
- **`n = 0`** はNバック構造もひっかけも作らない指示（後方互換用）

記号は文字列。数字なら `"5"`、位置ならマス番号 `"3"`、混合なら `"N5"` / `"P3"`。
生成側は中身を解釈しないので、2文字以上でも同じように扱える。

---

## ひっかけ (lure)

N-1 や N+1 の位置に同じ刺激を置く罠。**全方式で設定でき、既定は OFF。**

- **ON のときは決まった割合で意図的に置く**（既定 25%）。「許す」ではなく「置く」。
  そうしないと設定が効いているのか分からない
- **OFF のときは偶然生じるひっかけも排除する**（「足さない」ではなく「作らない」）。
  結果、実際に生じるひっかけは意図的に置いたものだけになり、偶然の一致と混ざらない
- ターゲットとは重ならない。ターゲット数は ON でも変わらない
- 記録には ON/OFF と、置いた数、**置いた位置そのもの**（`lurePositions`）を残す。
  あとから「この誤りは意図的なひっかけか、偶然か」を見分けられるようにするため

### 計算モードでは働かない

設定はできるが **OFF を勧める。**

> 答えが 0〜9 に限られるので偶然の一致と区別がつかないのでは、という懸念から検討した。
> ところが記号数の条件は「数字」課題（0〜9の10種）と同じで、
> ひっかけを置かないときの N±1 偶然一致率も 4.6%（1ブロック1.4件）で並ぶ。
> 範囲の狭さは計算に固有の問題ではなかった。
>
> 計算だけが違うのは、**繰り返される当のものが一度も目に入らない**こと。
> ひっかけが効くのは「同じものをまた見た」という親近性の信号が判断を狂わせるからだが、
> `21 ÷ 3` と `2 + 5` は答えが同じでも式はまったく別物で、見た目の反復が存在しない。
> しかも答えは知覚するものではなく計算して作り出すもので、
> 頭に載る頃には演算が終わっている。割り込む余地がない。
>
> 位置・数字・混合では意味がある。刺激そのものが再び目に入るので、
> 「さっきの5は2個前だったか3個前だったか」という**位置の取り違え**が実際に起きる。
> 古典的な誤警報の誘発とは働き方が違うが、負荷は上がる。

---

## モダリティ

| id | 内容 | 状態 |
|---|---|---|
| `visual-number` | 1〜9の数字を中央に表示（これまで慣れている形式） | 実装済み |
| `visual-position` | 3×3グリッドに四角が光る。中央を除いた8マスを使うのが標準 | 実装済み |
| `mixed-number-position` | 試行ごとに数字か位置かが切り替わる | 実装済み |
| `audio-letter` | 読み上げられる文字。子音のみ（C, H, K, L, Q, R, S, T）が定番 | 第2段階 |
| dual | position + audio を同時提示。判定は独立した2キーで別々に行う | 第2段階 |

この表はリアルタイム判定用。自分のペース方式の課題は
`calc-arith` / `paced-number` / `paced-position` という別立てで、この選択には出さない。

**混合モード**は仕様書になかったが追加した。記号は `N5`（数字の5）と `P3`（位置の3）の2種類で、
一致は**種類と値の両方**が同じときだけ。数字はグリッドの中央に出す
（中央マスは位置刺激には使わないので取り違えない）。
種類が混ざるぶん、同じ問題数でも位置単独より重い。

デュアルは「チャンネルを2本にする」だけで動く形にしてある。モダリティを2つ同時に**提示**するのがデュアル、
1つのモダリティの中で**切り替わる**のが混合。別物。

---

## タイミング

- 刺激提示 500ms、間隔 2500ms（1試行あたり計 3000ms）が標準。**設定可能にする。**
- 反応の受付は次の刺激が出るまで（リアルタイム判定のみ）
- リアルタイム判定の 1ブロック = N + 20 試行 程度
- 自分のペース方式にタイマーは無い。出題は N + 回答数（既定 15）
- ブロック開始前に3カウント

---

## 入力

**タップとキーの両方を常に用意する。** スマホで使うのが主なので、タップが第一。
キーは PC で使うときのため。どちらも同じ判定に入る。

### 自分のペース

- 画面下部の入力だけ。キーボードは使わない
- 計算・数字は 0〜9 のボタン
- 位置は3×3グリッド。**数字ボタンには置き換えない**（位置は位置で答える）
- 混合は位置グリッドと数字パッドを左右に並べる。スマホでは1マス50px前後になる
- 最初の N 問は「次へ」ボタンで進む
- 押した時点で確定して次へ（`answerDigits` を上げれば桁がそろってから確定）

### リアルタイム判定

- 画面下部の大きな「一致」ボタン、またはスペースキー
- 一致しないときは押さない（no-go 方式）
- 同一試行内の二重押しは無視
- デュアルでは左右2つのボタンになり、キーは 位置 = A、音 = L

押した合図は色の反転と、対応端末では短い振動。

---

## 判定と指標

### リアルタイム判定

**正答率だけでは評価しない。** 「一致と思ったら全部押す」戦略で数字が良く見えてしまい、
あとから自分の伸びを見るときに使えなくなる。

- ヒット率 = ヒット ÷ ターゲット総数
- 誤警報率 = 誤警報 ÷ 非ターゲット総数
- この2つを必ず別々に記録する
- d'（弁別力）も算出する

d' は対数線形補正つき。0%/100% でも無限大にならない代わりに、
ターゲット数と非ターゲット数が違うぶん、全押しでもちょうど 0 にはならず ±0.4 程度に出る。
弁別できていないことは読み取れるのでそのままにしてある。

### 自分のペース

「全部押す」に相当する抜け道がないので、正答率で評価してよい。
**ヒット率・誤警報率・d' は使わない**（記録では `null`）。

- 正答率 = 正答数 ÷ 問題数
- 誤答数
- 連続正答数（頭から何問保つか）
- 未回答は誤答として数える

平均反応時間は、リアルタイム判定はヒット時のみ、自分のペースは1問あたり。

---

## 記録

1行1ブロックの JSON Lines。`responseMode` で中身が分かれる。

共通:

- 日時 / 応答方式 / N / モダリティ / 試行数（または問題数）
- 平均反応時間 / 乱数シード
- 設定値（提示時間・間隔・ターゲット率・ひっかけの有無・中央除外・チャンネル構成）
- ひっかけの有無（`lure`）、置いた数（`lures`）、置いた位置（`lurePositions`）
- チャンネルごとの内訳（`channels[]`）。単一のときは先頭の値をトップレベルにも出すので、
  履歴の集計はチャンネル数に依存しない
- 刺激列そのもの（`symbols`）と、正解の位置（`isTarget`）

リアルタイム判定のみ:

- ターゲット数 / ヒット / ミス / 誤警報 / 正棄却
- ヒット率 / 誤警報率 / d'

自分のペースのみ:

- 課題（`task`）/ 問題数 / 正答数 / 誤答数 / 正答率 / 連続正答数
- 正解列（`expected`）と回答列（`answers`）と正誤（`correctFlags`）
- 計算課題なら出題した式そのもの（`problems`）

廃止した「提示後に入力」の記録（`responseMode: "recall"`）は読み込み時に捨てる。

古い記録や値が欠けた記録があっても、グラフや表が壊れないようにする
（欠けた点は打たない、集計から外す）。

保存先は **ブラウザ内 localStorage**。SQLite は使わない。
中身は JSON Lines と同じ形なので、`.jsonl` として書き出し／読み込みできる。
端末をまたぐときはこれで移す（履歴はブラウザごとに別）。

履歴画面は**応答方式ごとに区切って**表示する。
方式が違えば難易度のつまみ（N / 問題数）も指標も別物なので、同じ図に重ねない。
横軸を共有すると、片方の記録の位置にもう片方の穴が空くだけで、推移としても嘘になる。

**自分のペース方式は課題ごとにさらに分ける。** 位置のNバックと計算のNバックを
同じ図に重ねても読めない。

リアルタイム判定の区画:

- モダリティ別の N の推移
- ヒット率 / 誤警報率 の推移
- 日付ごと（最高N・ヒット率・誤警報率・d'）

自分のペースの区画（計算 / 数字 / 位置 / 混合 で1つずつ）:

- N の推移
- 正答率の推移
- 日付ごと（最高N・正答率）

成績の推移は**ひっかけの有無で線を分ける**。難易度が変わるので同じ線に混ぜると推移が読めない。
片方しか無いときは分けない。ブロック一覧にも「ひっかけ」列を出す。

共通:

- ブロック一覧。ここは集計ではなく時系列の記録そのものなので方式で切らない。
  行ごとに方式を出す。シードから再挑戦、削除もここから
- モダリティと方式での絞り込み。方式の選択肢は履歴の中身から作る

---

## 適応的難易度（第2段階以降）

Jaeggi らの方式: ブロック終了時に「ミス＋誤警報」が 3件以下なら N+1、5件以上なら N-1。

自分のペース方式は1ブロックの問題数を自由に決められるため、件数の閾値だと
問題数によって意味が変わってしまう。正答率で見て、90%以上なら N+1、70%未満なら N-1。

第1段階では**結果画面に目安を表示するだけ**。N も回答数も手動選択のままにして、
慣れてから自動化する。

---

## 着手前に決めたこと

1. **動作環境** — ブラウザ単体（HTML/JS、依存ライブラリなし・ビルドなし）。
   GitHub Pages に置いてスマホから使う
2. **音声の出し方** — 事前録音した音声ファイル。Web Speech API はブラウザ差が大きいので使わない
3. **保存先** — ブラウザ内 localStorage。`.jsonl` で出し入れする

---

## スマホで使うための条件

主な使用場面がスマホなので、これは付随ではなく要件。

- タップだけで一通り操作できること（キーは併用であって前提ではない）
- 回答は画面下部、片手の親指が届く位置と大きさ
- ブロック中は画面を消灯させない（Wake Lock）
- ホーム画面に追加して全画面で開けること（manifest）
- 電波がなくても起動できること（Service Worker でキャッシュ）
- **https で配信すること。** Wake Lock と Service Worker は保護されたコンテキストでしか動かない

---

## 第1段階のスコープ（完了）

- リアルタイム判定：視覚（数字・位置・混合）
- 自分のペース：計算 / 数字 / 位置 / 混合
- N と回答数は手動選択
- 1ブロック実行 → 結果表示 → 履歴保存
- スマホ対応（タップ入力・ホーム画面・オフライン）

音とデュアルは含めない。ただし「全体方針」のとおり、あとから足せる構造にしてある。

---

## やらないこと

ランキング、SNS共有、課金、他人との比較。
