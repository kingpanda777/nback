# CLAUDE.md

自分専用のNバック課題アプリ。ブラウザ単体（HTML/JS、依存ライブラリなし・ビルドなし）。
主な使用場面はスマホ。

- **設計と、なぜそうしたか** → [nback-spec.md](nback-spec.md)
- **実装の構造と拡張の仕方** → [README.md](README.md)

このファイルには、上の2つに書いていない「作業するたびに効く決まりごと」だけを置く。

---

## 変更したら必ずやること

1. `node test/core.test.js` を通す
2. **`sw.js` の `VERSION` を1つ上げる**
3. push する（公開先は GitHub Pages）

VERSION を上げ忘れると、スマホに古いキャッシュが配られ続けて更新が届かない。
ブラウザは `sw.js` の中身が変わったときだけ更新処理を始めるため。

反映は「次に開いたとき」。実機では**一度開いて閉じ、もう一度開くと**新しくなる
（1回目は裏で新版を取り込み、2回目からそれを配る作り）。

ドキュメントだけの変更なら VERSION は不要（`sw.js` の `ASSETS` に入っていない）。

公開URL: `https://kingpanda777.github.io/nback/`

---

## 動かす・確かめる

```bash
python -m http.server 8123
```

`.claude/launch.json` に設定済み。以下は実際に何度も引っかかった点。

- **ブラウザのHTTPキャッシュが古い JS/CSS を返す。**
  `python -m http.server` は `Cache-Control` を返さないので、同じポートで再読み込みしても
  更新が反映されないことがある。**検証は別ポートを立てて新しいオリジンでやる。**
  ページURLにクエリを付けても、スクリプトのURLは変わらないので効かない。
- **タップの検証は当たり判定まで見る。** `el.dispatchEvent(new PointerEvent(...))` は
  要素に直接届くので、上に何か被さっていても通ってしまう。実機で押せるかは
  `document.elementFromPoint(x, y)` がその要素を返すかで確かめる。
- 自動検証中、非表示タブではタイマーが強く節流される（1秒〜1分に1回）。
  長いブロックは実時間では終わらない。`setTimeout` を差し替えて一気に走らせる。

---

## 壊さないこと

過去に実際に壊した、または明示的に決めたもの。

- **`[hidden]` は作者スタイルの `display` に負ける。**
  `[hidden] { display: none !important; }` を全体に置いてある。
  個別に `display` を足すときは、その要素を `hidden` で隠していないか確かめる。
  これを踏んだとき、透明なオーバーレイが入力を全部吸い取ってタップが一切効かなくなった。
- **刺激列の生成（`core.js`）は提示方法を知らない。** 記号の配列を受け取って記号の列を返すだけ。
- **リアルタイム判定はヒット率と誤警報率を必ず別々に持つ。** 1つの正答率にまとめない。
  「一致と思ったら全部押す」戦略を見抜けなくなる。
- **履歴は方式・課題ごとに区切る。** 指標も難易度のつまみも別物なので同じ図に混ぜない。
  横軸を共有すると、片方の記録の位置にもう片方の穴が空くだけで推移として嘘になる。
- **自分のペース方式で N個前との一致を禁じない。** 禁じると
  「いま出ているものは答えではない」と分かってしまい、選択肢が狭まる。
- **自分のペースの課題IDは `modalities.js` のIDと別にする。**
  同じ「数字」でもリアルタイムは1〜9、自分のペースは0〜9で記号集合が違う。
  同じIDにすると記録を後から読むときに取り違える（一度やって履歴の表示が壊れた）。
- **ひっかけ OFF は偶然の一致も作らない（0件）。** ON は「許す」ではなく「置く」。
- **廃止した `recall` の記録は `storage.js` の読み込み口で落とす。**
  落とすのを1か所に寄せてあるので、履歴側は廃止方式を知らなくてよい。
- **回答は必ず画面下部、タップだけで完結すること。** キーは併用であって前提ではない。
- グラフに欠けた値を渡さない。`NaN` を座標にすると SVG が壊れてコンソールにエラーが出る。

---

## 決めたこと（蒸し返さない）

- **動作環境** — ブラウザ単体 + localStorage。Python + 静的サイト案は不採用
- **音声** — 事前録音した音声ファイル。Web Speech API はブラウザ差が大きいので使わない
- **「すべて覚えて最後に全部答える」方式は廃止** — Nバックではなく記憶スパン課題で、
  N の出番が無かった。記録も残さない
- **計算モードの ひっかけ は効かない** — 式は毎回違う見た目になり、繰り返されるのは
  目に入らない「答え」だけ。設定はできるが OFF 推奨
- **「1つ戻る」は自分のペース方式に付けない** — 前の刺激を出し直すことになり、
  見直しの時間を与えて課題の性質が変わる
- **適応的難易度は表示のみ** — N も回答数も手動選択のまま

---

## やらないこと

ランキング、SNS共有、課金、他人との比較。

---

## 現在の状態と次

第1段階は完了。

- リアルタイム判定（no-go）— 数字 / 位置 / 混合
- 自分のペース — 計算 / 数字 / 位置 / 混合
- ひっかけは全方式で設定可（既定 OFF）
- スマホ対応（タップ入力・ホーム画面追加・オフライン）

次は第2段階（音声・デュアル）。`js/modalities.js` に `audio-letter` を1つ登録し、
`buildConfig()` の `channels` を2要素にするところから。手順は README。
事前録音の音声ファイルが要る。
